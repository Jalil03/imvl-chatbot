# Transformers + HF ecosystem
transformers==4.35.2
accelerate==0.27.2
huggingface_hub==0.20.3
sentencepiece==0.1.99
datasets==2.14.6
peft==0.6.2

# Optional optimizations (pinned for torch 2.1 + cu118)
xformers==0.0.22.post7
bitsandbytes==0.41.3

# Utility and logging
tqdm==4.67.1
scipy==1.11.4
numpy==1.26.4
rich==13.7.1

# (Optional) GPTQ — only if you truly need it
# auto-gptq==0.6.0
# If you enable auto-gptq later, install it separately AFTER the rest.
